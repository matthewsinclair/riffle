# Implementation - ST0001: Extricate Predicate and SIA from Multiplyer

## Implementation

### WP-01 -- D2 root-cause (as-built)

Method: five characterisation tests re-run in the archive (12 passed, pins hold), then a stage-by-stage probe script (`mix run`, scratchpad-hosted, zero archive edits) against the 4-row characterisation dataset, then git pickaxe forensics. Full verdict + evidence: design.md "D2 verdict". Headline: the engine produces correctly tagged sense->infer->act items; the glue's `process/4` discards them after counting -- the `:results` cargo writes were deliberately removed in archive commit `e0b5dc2a` (2025-04-04). Nothing travelled with the port.

### WP-02 -- Gate (as-built)

`mix gate` alias in mix.exs = `format --check-formatted` + `compile --warnings-as-errors --force` + `test --warnings-as-errors`, with `preferred_envs: [gate: :test]` via `cli/0`. `.github/workflows/ci.yml` runs the same alias on erlef/setup-beam (Elixir 1.20.3 / OTP 29, matching the local toolchain) with deps/_build caching. One gate definition, two callers (Highlander). First upstream Actions run lands on the next hv push.

### WP-03 -- Mechanical port (as-built, commit 7b7f912)

- 15 engine files (~5.1k LOC) -> `lib/riffle/predicate/`; 21 test files (~3.5k LOC) -> `test/riffle/predicate/`; 2 data fixtures (`user_test.pred`, `users_small.csv`) -> `test/fixtures/`.
- Rename: sed over the copied tree (`Multiplyer.Predicate` -> `Riffle.Predicate`, `Multiplyer.{` -> `Riffle.{` for doctest multi-aliases). Residue after sed was exactly the 8 stitch lines the handoff predicted (pipeline.ex x4, loop.ex x3, macro.ex comment x1) -- severed by hand, by design not sed.
- Stitch severance (DD-1):
  - `Riffle.Predicate.default_pipeline/0` -- the one config surface (`Application.get_env(:riffle, :default_pipeline)`), nil when unset.
  - pipeline.ex both process-clause sites: `module = Map.get(pipeline, :module) || Riffle.Predicate.default_pipeline()`. The magic pattern-layer name list (`[:main, :sense_pipeline, :infer_pipeline]`) is gone; explicit module now outranks the default (the archive inverted that).
  - loop.ex reference resolution: the six silent always-false fallback shapes replaced by `resolve_reference!/2` + `Riffle.Predicate.UnresolvedPredicateError` raises naming the predicate, the module, and (when relevant) the config key. Try/rescue swallows around resolution are gone; real errors propagate.
- `Riffle.Application` supervises `Riffle.Predicate.Cache` (the archive app tree did the same); `mod:` added to mix.exs.
- D5: `expr_macro_direct_test.exs` piped the function into `Predicate.new/3` first position, making `name` a function and `function` a string -- a predicate that would crash if evaluated, which the suite therefore never did. Fixed to `new(name, description, function)`.
- Gate at land: 237 passed (61 doctests, 176 tests), zero warnings.

### WP-03 -- Remediation pass (as-built)

Two critics ran on the ported tree: code review (5 CRITICAL, 12 WARNING, 5 recommendation) and test-check (6 CRITICAL, 3 WARNING, 29 recommendation). All 11 CRITICALs fixed in three layered commits, gate green at each:

- **R1 (29eac91) -- engine criticals.** Cache keyed on the exact `{predicate_id, item}` term (the 3-field sample hash returned other rows' cached results on collision -- the production-triage finding). `create/1` has one loud failure contract (block-eval and catch-all no longer degrade to always-false; reraise carries the body AST). Registry `ensure_*_struct` resolves references against registry state with tagged errors (always-true stubs and empty-loop synthesis gone). Malformed comparison dates raise at predicate construction. DefaultPipelineConfig probes `function_exported?` instead of rescuing around the whole pipeline invocation.
- **R2 (335a655) -- hydrate-at-generation + un-neutered tests.** Macro-generated loop/pipeline functions resolve references through the defining module and hydrate bodies to callables once at generation; missing names raise `UnresolvedPredicateError`. This was the root of the archive's "skip filtering for now (structural issues)" test-neutering: pipelines carried unresolved reference maps that silently always-falsed. The four neutered tests now run real `Loop.filter` / `Pipeline.process` with exact survivor and tag pins; the loop-cache short-circuit hedges became exact pins (`Loop.process` evaluates every predicate; the cache is per `{predicate, item}`).
- **R3 (bea85fa) -- test structure.** `Cache.reset_stats/0` + `Cache.config/0` public API replaced three verbatim `:sys.replace_state` blocks; registry/loader fixtures moved to per-test ExUnit `tmp_dir` (shared destructive path gone, registry suite async: true); async opt-outs carry explanatory comments; predicate_test restores prior cache config.

Structural WARNINGs (six hydration sites with four failure behaviours, Loop's dual evaluation path bypassing the cache on streams, the STD module twin, Evaluator/StandardLib coercion drift, the five-file expr-macro test family) are dispositioned to WP-04 under DD-7, not silently dropped. Suppressed-recommendation triage also deferred there.

### WP-04 -- PFIC transform + hydration consolidation (as-built)

Socrates design pass ran first (advisory, per the WP charter); its verdicts, agreed signatures, and implementation order are DD-9. hv rulings applied at start: DD-8 coercion strict; push authorised. Ten commits, `mix gate` green at every one:

- **c1 (5acf8e8) Resolver.** `Riffle.Predicate.Resolver`: tagged core (`resolve_predicate/loop/pipeline`) + bang wrappers; lazy default-pipeline fallback confined to `lookup/3` (inline bodies resolve without touching config); seen-path cycle guard; structs re-walked; no benign catch-all. Red-first: 27 tests. `Predicate.create/1` absorbs the loader's raw `{:expr, meta, [expr]}` head; expr parse raises unified to ArgumentError.
- **c2 (4fdbb66) Macro layer.** Generated loop/pipeline functions call `resolve_loop!/resolve_pipeline!(__MODULE__, def)`; the `hydrate_*_ref!` twins die.
- **c3 (ae19111) One evaluation entry point (AC-04.3).** `Loop.filter/2` = Stream over `Loop.process/2` (cached); map shapes resolve ONCE before streaming; `evaluate/2` head requires `function:` (breach fires before a phantom cache miss); per-item body hydration and reference clauses deleted; `evaluate_direct` private. AT-04.3 red-first: the stream path was re-evaluating cached items.
- **c4 (4c88826) Pipeline.** Six-clause maze -> resolver normalisation + exactly two process clauses (`reduce_while` single item, `Enum.reduce` of `Loop.filter` for streams). Deleted with zero callers: `hydrate_pipeline/hydrate_loop/hydrate_predicate` (no nil-checks, bare RuntimeErrors), `validate_has_main_pipeline` (archive `:main` convention), `create/2,3`, `filter/2` alias.
- **c5 (dcc43c0) Registry.** Getters are tagged Resolver calls over state; `get_predicate` returns the hydrated definition map -- the callable wrapper was lossy (dropped the tagged item) with a type-drifting fallback; `{:not_found, ...}` -> `{:unresolved, kind, name, :definitions}`; `ensure_*`/`resolve_*`/`fetch_*` privates die.
- **c6 (8168ea0) Loader.** `create_instances` builds predicates -> loops -> pipelines, each phase resolving against the accumulated instances; unresolved refs are tagged errors, never nil entries; `create_predicate_function` (incl. the dead `{:call, ...}` clause) deleted; broad rescue narrowed to ArgumentError -> `{:invalid_predicate_body, msg}`. Found en route: the "handles inline definitions" test was green over a rotten instance (cross-file refs loaded from one file became nil predicates); rewritten against the full directory with exact leaf pins, plus a red-first unresolved-ref test.
- **c7 (5e196fb) STD twin.** `Dsl.STD` (a name-shadowing delegate subset -- both aliases bound `STD`, the second silently won) and the `use`-helper module deleted; STD = the StandardLib alias, injected once by macro/loader; parse-level STD tests moved to loader_test.
- **c8 (682824f) Silent drops raise.** Parser in-block catch-alls and macro defloop/defpipeline extractors raise naming the offending statement (`Macro.to_string`); the loader converts to `{:invalid_dsl, msg}` at the user-input boundary. Top-level extract catch-alls stay -- they are kind-selectors, not drops.
- **c9 (c0d80fa) Expr test family.** Five files / 17 tests -> one canonical `expr_test.exs` / 6 strong tests with ONE shared in-file DSL fixture module. The loop/pipeline tests now exercise the real macro-generated path -- the deleted hand-rolled resolution helpers existed only because that path was broken pre-hydration. AC-04.4 support-helper judgement: in-file fixture module, no `test/support` indirection -- single consumer, fixtures closest to use.
- **c10 (577d4f5) Strict coercion (DD-8, AC-04.5).** `Riffle.Predicate.Coerce`: full parses only, tagged returns, one truthiness enumeration (union of the two drifted sets -- `t` was truthy only in the Evaluator, `on` only in StandardLib). Evaluator raises `CoercionError` on explicit-coercion garbage; `create_function/1` converts exactly that to no-match -- `to_integer(@garbage) == 0` no longer matches. Implicit mixed-type comparisons are full-parse-or-false. StandardLib numeric/boolean route through Coerce; `parse_number` and the private truthy/falsey twins die.

Suite at c10: 274 passed (66 doctests, 208 tests). Follow-ups filed in DD-9, out of scope here: Cache perf (GenServer call per lookup -- the flaw the archive's uncached stream fork was protecting), cache keys not source-qualified (flat predicate namespace).

### WP-04 -- Critic re-runs + R4 remediation (as-built)

Two critics ran on the transformed tree (AC-04.2 evidence). **Test-check: 0 CRITICAL, 2 WARNING** -- both IN-EX-TEST-007 (cache-setup triplicated across three suites; DSL fixture corpus with no canonical home). **Code review: 0 CRITICAL, 6 WARNING** -- 4x module-highlander (DefaultPipelineConfig reflection as a second registry; Evaluator text/equality/logical operators outside Coerce; the comparison-ladder triplication; the macro/parser extraction twin), 1x downgraded no-silent-failures (parser TOP-level junk vanishes -- in-block already raises), 1x PFIC (within_last_days reads the wall clock inside a cached closure: results could flip at midnight mid-TTL).

**R4a (d8cac87), fixed:** `test/support/` lands (`elixirc_paths(:test)`): `Riffle.CacheHelpers` (reset_cache/1, spy_predicate/4) + `Riffle.DslFixtures` (canonical UserPredicates module, basic/trial/complex .pred sources); six suites route through them; two oracle recommendations taken -- the exhaustive `list_pipelines` pin (which immediately exposed the reflection leak as a REAL defect: loops and predicates listed as pipelines, `get_pipeline(:test_loop)` returning a loop) and a literal 26-suspended pin replacing a mirror-logic oracle. DefaultPipelineConfig membership now comes from the canonical `pipelines()` map. Evaluator operator layer completed onto Coerce: new `Coerce.to_text/1` (nil never becomes `""`), text ops no-match on non-text (StandardLib.Text parity -- `starts_with(@missing, "")` no longer matches), `missing == missing` is false, logical operands go through the truthiness enumeration (`!@missing` no longer matches; unenumerated strings raise CoercionError -> no-match), one `coerced_compare/3` ladder + one `coerce!/2` boundary replace the triplicated case scaffolding. `within_last_days` binds its window at construction. Suite: 282 passed (67 doctests, 215 tests).

**R4b (7c3c940), fixed:** `Riffle.Predicate.Dsl.Statements` -- THE block-grammar ladder (`block_statements/1`, `predicates!/2`, `loops!/3`; context words as data, raise-message pins verbatim) -- replaces the macro/parser twins and their eight dead expr clauses. Parser rebuilt on one top-level dispatch (`extract_definitions!/1`) whose catch-all IS the completeness check; deliberate deviation from the restart.md accept-list design, which would have passed recognised-head-malformed-shape statements (eg a bare top-level `predicate(:x)`) straight into the kind-selector drops. Loader thins to parse -> extract. The re-run test-check found 1 CRITICAL -- the STD-alias test pinned parse-level fields only, provably passing with the alias injection deleted -- and strengthening it exposed the STD surface as broken: `call` syntax had NO handler (`undefined function call/2`; the archive's always-false catch-all had swallowed exactly this), and the loader's alias-injection prelude was theatre (parsing never resolves aliases). Fixed at the one body-to-function home: `Predicate.create/1` gains the `{:call, ...}` head (builder capture applied to literal args, validated 1-ary, loud otherwise) and `expand_std/1`, a pure AST prewalk binding STD before every body eval (eval-env alias options are deprecated on 1.20); the injection is deleted and `.pred` alias statements now reject at the completeness check rather than accept-and-ignore. R4b verification: code critic on the four DSL files 0 CRITICAL / 0 WARNING (Highlander + PFIC verified structurally); test-check recommendations taken (exact `load_directory` name pins, assert-wrapped act matches, `extract!/1` helper, TokenMissingError message pin). Gate: 286 passed (67 doctests, 219 tests), zero warnings; R4b is net -50 lines.

**Deferred with disposition:** per-file test specs via diogenes (suite-wide process change, not WP-04 scope; candidate alongside ST0003 test discipline); 9 lib RECOMMENDATIONs triaged to backlog, incl. a socrates handoff on whether accessor generation should live in exactly one of `Dsl.Macro.__before_compile__` / `DefaultPipelineConfig` (their `get_pipeline/1` return shapes diverge -- resolved struct vs raw map -- currently masked by the Resolver's re-walk). The R4b critic added two parser-boundary RECOMMENDATIONs, both deferred with its own calibration reasoning on record: a single definition-argument-shape recogniser in `Statements` (the top-level and in-block ladders share the `name x description? x [do: body]` production; divergence fails loud today, so bounded shared knowledge behind a deliberate boundary -- the suggested socrates question is filed with it) and one error vocabulary for malformed DSL text (`Parser.parse/1` returns the raw exception struct where extraction failures return `{:invalid_dsl, message}`; unifying changes `load_string/1`'s public error shape, a deliberate-contract decision, not a close-out edit).

## Technical Details

- Loop/pipeline resolution failure semantics changed from silent-degrade to raise. The engine's own last-resort (`evaluate_direct` on an unresolved reference) already raised; resolution now matches it.
- The zero-trace gate (AT-03.2) assembles its needle at runtime (`"multi" <> "plyer"`), so it scans every file under lib/ and test/ with no exclusions, itself included.
- `.pred` fixture usage-example comments were renamed with the API (they document the current API truthfully).

## Challenges & Solutions

- Ported suite initially failed 62 tests: the archive's test_helper ran `Application.ensure_all_started(:multiplyer)` and its app tree supervised the engine cache. Solution: minimal `Riffle.Application` supervising the cache -- not a test_helper hack -- because the cache is engine infrastructure, not test scaffolding.
